<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ProductListActivity">

    <!-- Información de búsqueda -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardBusqueda"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="8dp"
        app:cardElevation="4dp"
        app:cardBackgroundColor="@color/primary_light"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/tvBusquedaTitulo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/resultados_para"
                android:textStyle="bold"
                android:textSize="18sp"
                android:textColor="@color/primary"
                tools:text="Resultados para 'leche'" />

            <TextView
                android:id="@+id/tvTotalProductos"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/productos_encontrados"
                android:layout_marginTop="4dp"
                android:textSize="14sp"
                tools:text="15 productos encontrados" />

            <TextView
                android:id="@+id/tvSupermercados"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/supermercados_consultados"
                android:layout_marginTop="4dp"
                android:textSize="12sp"
                android:textColor="@color/gray_dark"
                tools:text="Supermercados consultados: La Reina, Carrefour" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

    <!-- Lista de productos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvProductos"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="16dp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/cardBusqueda"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_producto" />

    <!-- Indicador de carga -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/rvProductos"
        app:layout_constraintBottom_toBottomOf="@id/rvProductos"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Mensaje sin resultados -->
    <TextView
        android:id="@+id/tvSinResultados"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/sin_resultados"
        android:textSize="16sp"
        android:textColor="@color/gray_medium"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/rvProductos"
        app:layout_constraintBottom_toBottomOf="@id/rvProductos"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botón flotante para ver lista de compras -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabVerLista"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:src="@drawable/ic_shopping_cart"
        android:contentDescription="Ver mi lista de compras"
        app:tint="@android:color/white"
        app:backgroundTint="@color/primary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>